server:
  port: 8090

# if we need customize routing config we will write all routes here and disable eureka ribbon service
# config example will be commented because we haven't any working service, it's only test

#zuul:
#  routes:
#    cloud-eureka-client:
#      serviceId: cloud-eureka-client
#      path: /greeting/*
#    feign-client-example:
#      serviceId: feign-client-example
#      path: /greeting/*
#
#ribbon:
#  eureka:
#    enabled: false
#
#cloud-eureka-client:
#  ribbon:
#    listOfServers: http://localhost:8002 ,
#      http://localhost:8003
#
#feign-client-example:
#  ribbon:
#    listOfServers: http://localhost:8094